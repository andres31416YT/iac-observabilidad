groups:
- name: cpu-alerts
  rules:
  - alert: HighCPULoad
    expr: changes(cpu_load_percent[30s]) > 0
    for: 10s
    labels:
      severity: warning
    annotations:
      summary: "CPU load detected"
      description: "CPU load endpoint was activated"